# Computer-Graphics

### PA0 Q&A
* 问题1：画线时采用书中P23的改进Bresenham算法时，如果dx<0则无法正常绘制
    * 解决方案：从左往右画，此时y有问题，只需要加一个step并且在判断时注意一下即可
* 问题2：画圆时圆不像圆
    * 解决方案：公式推错，所有关于d的值均需要乘4，并且在d的判断中需要把x - cx置为新x，y - cy置为新y
* 问题3：区域填充扫描线算法编译不过
    * 解决方案：判断边界，注意边界是0 <= x < r
* 问题4：自己测试时发现利用原颜色填充新颜色会死循环
    * 解决方案：判断